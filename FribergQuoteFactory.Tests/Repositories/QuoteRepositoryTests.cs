
namespace FribergQuoteFactory.Tests.Repositories
{
    public class QuoteRepositoryTests
    {

        [Fact]
        public async Task AddQuote_WithValidQuote_AddsQuoteToRepository()
        {
            /*
            // Arrange

            var quoteRepository = new QuoteRepository();

            var newQuote = new Quote
            {
                Id = Guid.NewGuid(),
                Quote = "Carpe Diem",
                Category = "motivation",
                Approved = false,
            };

            // Act
            quoteRepository.Add(newQuote);

            // Assert
            Assert.Contains(newQuote, await quoteRepository.GetAllAsync());
            */
        }
    }
}
